PROJECT(tests)

FILE(GLOB PROJECT_SOURCES "*.cpp")
FILE(GLOB PROJECT_INCLUDES "*.h")


ADD_LIBRARY(tests STATIC ${PROJECT_SOURCES} ${PROJECT_INCLUDES})

SET_TARGET_PROPERTIES(tests PROPERTIES PROJECT_LABEL "tests common tools" )
