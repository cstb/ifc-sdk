FILE(TO_CMAKE_PATH ${CMAKE_HOME_DIRECTORY}/CMake ELCPP_MODULE_PATH)
SET (CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} ${ELCPP_MODULE_PATH})

FILE(TO_CMAKE_PATH ${CMAKE_HOME_DIRECTORY} ELCPP_INSTALL_PREFIX)
SET (CMAKE_INSTALL_PREFIX ${ELCPP_INSTALL_PREFIX} CACHE PATH ${ELCPP_INSTALL_PREFIX})

SET(EXECUTABLE_OUTPUT_PATH ${CMAKE_HOME_DIRECTORY}/bin CACHE PATH  ${CMAKE_HOME_DIRECTORY}/bin)
SET(LIBRARY_OUTPUT_PATH ${CMAKE_HOME_DIRECTORY}/lib CACHE PATH ${CMAKE_HOME_DIRECTORY}/lib)

INCLUDE_DIRECTORIES(${CMAKE_HOME_DIRECTORY}/include)

PROJECT(eve_ifc2x3)
SUBDIRS(src)

FIND_PACKAGE(Step REQUIRED)

FIND_PACKAGE(Delta3D)
IF(NOT DELTA3D_FOUND)
MESSAGE("WARNING: Delta3d not found. Trying to Use log4cplus instead.")
FIND_PACKAGE(log4cplus REQUIRED)
ENDIF(NOT DELTA3D_FOUND)

SET(CMAKE_DEBUG_POSTFIX "D")
